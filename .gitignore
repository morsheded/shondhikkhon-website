# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.env*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# cloudflare
.wrangler
.vercel/output

# raw/uncompressed source images — tracked in LFS or stored externally
# images_for_site/ is intentionally NOT ignored so it stays as source of truth
# but you may want to add it if the repo becomes too large:
# /images_for_site/

# Docker build context file
# .dockerignore is tracked in git so it's part of the repo
